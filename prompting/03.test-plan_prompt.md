# 프롬프트 기록: test-plan

## Export 원칙

- 이 파일은 사용자 Prompt와 AI 답변의 전체 Export 기록이다.
- 요약본이 아니라 원문 보존을 우선한다.
- 숨겨진 시스템 메시지와 비공개 내부 추론은 제외한다.

## 대화 전문

### Turn 1 - User Prompt

```
[P] 시니어 QA 리드입니다.
[C] C++17, Google Test, CMake, (가능하면 gcov/lcov)
[T] 테스트 계획서를 작성해줘.
    - 요구사항분석에 적힌 각 케이스의 경계값 포함
    - 다음선호채널 버튼을 연속해서 눌렀을때의 동작검증
    - 선호채널이 없을때의 동작 검증
    - 기타입력 버튼 동작 검증
    - 기타 요구사항을 만족시키는지 확인하기 위한 테스트 포함
   
[F] Markdown 문서. docs 폴더에 test_plan.md로 저장

```

### Turn 1 - AI Answer

요구사항 분석과 README를 확인한 뒤 QA 관점 테스트 계획서를 작성했습니다.

**산출물**: `docs/test_plan.md`

**문서 구성 요약**:

| 섹션 | 내용 |
|------|------|
| **1~3** | 범위, gcov/lcov 설정, Mock·Fixture·경계값 분류 |
| **4** | 숫자 채널 변경 (REQ-CH): README 시나리오 + `0`/`99`/선행 0/3번째 숫자/무효 채널 |
| **5** | 선호 채널: 토글, **FAV_NEXT 단일·연속(FAV-C01~C13)**, **빈 목록(FAV-E01~E04)** |
| **6~7** | SEARCH, 업/다운(검색 유·무, wrap 경계) |
| **8** | 기타 버튼 1회 입력, 버퍼 충돌(BTN-X*), E2E 복합 |
| **9~12** | RTM, TDD 로드맵, 출구 기준, `remoteKey` 확장 체크리스트 |

**요청 항목 반영**:

1. **경계값**: 채널 `0`/`99`, Tuner `ValidChannels`/`InvalidChannels`, 업/다운 wrap, fav `{0,50,99}` 등 TC-ID로 명시  
2. **다음선호 연속 입력**: §5.3 `FAV-C01~C13` — press마다 CH 시퀀스·`InSequence` Mock 예시  
3. **선호 없음**: §5.4 `FAV-E01~E04` — no-op(POL-02) 및 add 후 복구  
4. **기타 버튼**: §8 (SEARCH, UP/DOWN, 버퍼 무효화)  
5. **도구**: GTest/gmock, CMake, gcov/lcov 명령·커버리지 목표 포함  

구현 전 **POL-01~04**(빈 버퍼 OK, 빈 fav FAV_NEXT 등)는 권장 기대를 적어 두었습니다. 구현 정책이 정해지면 해당 표만 맞추면 됩니다.

### Turn 2 - User Prompt

```
보고서작성 및 백업
```

### Turn 2 - AI Answer

(본 턴: report-backup-agent 루틴 수행 — 보고서·프롬프트 백업·GitHub 업로드)

## 실행 명령 및 도구 출력

### 프로젝트 탐색 (Turn 1)

- `Read` — `docs/requirements_analysis.md`, `prompting/02.requirements-analysis_prompt.md`
- `Glob **/*.{cpp,h,hpp,cmake,txt,md}`
- `Read` — `README.md`, `include/remoteKey.h`, `test/TunerTest.cpp`, `test/TVControllerTest.cpp`

### 파일 작성 (Turn 1)

- `Write` → `docs/test_plan.md` (약 418행)

### Git 상태 확인 (Turn 2)

```text
On branch prompting
Your branch is up to date with 'origin/prompting'.

Untracked files:
  docs/test_plan.md
```

Recent commits:
```text
8567145 Update report with commit hash and push result
ab3be8f Add requirements analysis doc with report backup
b962812 Add report backup agent
```

### 빌드 및 테스트 (Turn 2)

```powershell
Set-Location "e:\dev\TDD_TV_13"
cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug
cmake --build build
ctest --test-dir build --output-on-failure
```

결과:
```text
100% tests passed, 0 tests failed out of 14
Total Test time (real) = 1.11 sec
```

## 최종 상태

- `docs/test_plan.md` 작성 완료
- `report/03.test-plan_report.md` 작성 완료
- `prompting/03.test-plan_prompt.md` 작성 완료
- 커밋·Push: (Turn 2 완료 후 갱신)

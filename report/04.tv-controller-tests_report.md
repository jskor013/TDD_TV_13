# 작업 보고서: tv-controller-tests

## 개요

- 작업 일시: 2026-05-19
- 작업 목적: `docs/test_plan.md`를 근거로 `TVController` Google Test/gmock 단위·통합 테스트를 작성하고, `cmake --build build && ctest` Green을 달성한다.
- 핵심 결과: `TVControllerTest` 68케이스 + 파라미터 6케이스 추가(총 81 테스트), `remoteKey` 전체 확장 및 `TVController` 구현 완료, 전체 테스트 100% Pass.

## 변경 내용

- 수정/추가한 파일:
  - `include/remoteKey.h` — `KEY_0`~`KEY_9`, `KEY_OK`, `KEY_CH_UP/DOWN`, `KEY_SEARCH`, `KEY_FAV_ADD/NEXT` 및 `isDigitKey`, `digitFromKey` 헬퍼
  - `include/TVController.h` — 숫자 버퍼·2자리 커밋·선호 채널·채널 검색·업/다운(검색 유·무) 구현
  - `test/TVControllerTest.cpp` — `TVControllerFixture`, REQ-CH/FAV/SEEK/UD/BTN/E2E 테스트 (약 550행)
  - `report/04.tv-controller-tests_report.md` (본 보고서)
  - `prompting/04.tv-controller-tests_prompt.md` (대화 백업)
- 주요 구현 내용:
  - 숫자 입력: 1자+OK, 2자 즉시 커밋, 선행 0(`stoi` 정규화), 3번째 숫자 대기, 기타 키 시 버퍼 클리어
  - 선호 채널: `FAV_ADD` 토글, `FAV_NEXT` (min{f>current} 또는 wrap), 빈 목록 no-op (POL-02)
  - 채널 검색: `seekCH()` 반복 until `""`, 목록 덮어쓰기
  - 업/다운: 검색 없음 `(ch±1+100)%100`, 검색 있음 목록 내 next/prev + wrap
  - `Tuner.h`는 수정하지 않음
- 테스트 변경 내용:
  - `TunerTest.cpp` 비수정 (기존 13케이스 유지)
  - `TVControllerTest` 스켈레톤 → test_plan P0/P1 대표 TC 구현
  - Stateful Mock: `ON_CALL(setCH)`로 `currentChannel_` 갱신, `FAV_NEXT` 연속 입력 검증
  - CH-S03: 플랜의 "버퍼 3 + KEY_1"은 `"31"`이 되므로 `KEY_1` → `KEY_3` → `"13"`으로 조정

## 검증

- 실행한 빌드 명령:
  - `cmake --build build`
- 실행한 테스트 명령:
  - `ctest --test-dir build --output-on-failure`
- 테스트 결과:
  - 81개 테스트 모두 통과 (TunerTest 13 + TVControllerTest 68)
  - 실패 테스트 0개
- 커버리지 확인 결과:
  - 이번 작업에서는 gcov/lcov 커버리지를 별도로 실행하지 않았다.

## GitHub 업로드

- 브랜치: `prompting`
- 커밋 메시지: `Implement TVController tests with report backup`
- 원격 저장소: `origin` (`https://github.com/jskor013/TDD_TV_13.git`)
- Push 결과: 성공 (`11be2ff..fd991b4` → `origin/prompting`)
- 커밋 해시: `fd991b4`

## 주의 사항

- 남은 리스크:
  - test_plan P2 일부 TC 미구현 (FAV-C06 ×10, SEEK-03/04, UD-T02, FAV-A03~A06, CH-INV-02 등)
  - `std::cout` 로그는 비관측이나 구현에 남아 있음
- 후속 작업:
  - (선택) ENABLE_COVERAGE 빌드 및 `TVController` line coverage ≥ 90% 검증
  - 미구현 P2 TC 추가 및 `dev` 브랜치 머지 검토

# 작업 보고서: texttest-golden-master

## 개요

- 작업 일시: 2026-05-19
- 작업 목적: `TextTestFixture` 출력 기반 Golden Master(Approval) 회귀 테스트를 설계·구현하고, CMake/ctest·CI에 통합한다.
- 핵심 결과: README 대표 시나리오 12건에 대한 golden 파일(`test/golden/`) 및 `TextTestGoldenTest` 추가, 전체 **93개 테스트 100% Pass**, GitHub Actions CI 워크플로 추가.

## 변경 내용

- 수정/추가한 파일:
  - `CMakeLists.txt` — `TextTestGoldenTest` 타깃, `UPDATE_GOLDEN_MASTER` 옵션, `update-golden` 커스텀 타겟, `gtest_discover_tests`
  - `.gitignore` — `test/golden/**/*.received.txt` 제외
  - `.github/workflows/ci.yml` — push/PR 시 configure → build → ctest
  - `scripts/update_golden.ps1`, `scripts/update_golden.sh` — golden 재생성 스크립트
  - `test/support/CoutCapture.hpp` — `std::cout` 캡처
  - `test/support/GoldenMaster.hpp` — golden 읽기/비교/갱신 (`GOLDEN_UPDATE=1` 또는 `UPDATE_GOLDEN_MASTER`)
  - `test/support/FakeTunerForText.hpp` — 시나리오용 Fake Tuner
  - `test/TextTestGoldenTest.cpp` — `TextTestFixture` 및 README 시나리오 12 테스트
  - `test/golden/TextTestFixture/*.golden.txt` — 기준 transcript 12개 (Git 커밋 대상)
  - `report/06.texttest-golden-master_report.md` (본 보고서)
  - `prompting/06.texttest-golden-master_prompt.md` (대화 백업)
- 주요 구현 내용:
  - **보관 전략**: `test/golden/<TestSuite>/<TestName>.golden.txt`, 실패 시 `*.received.txt` 생성
  - **Transcript 형식**: `> KEY` 입력 줄 + `TVController`의 `std::cout` 로그 (`현재 설정하는 채널 : N`)
  - **역할 분리**: 기존 `TVControllerTest`(Mock/`setCH` 계약) vs Golden(로그 출력 회귀)
  - `Tuner.h`는 수정하지 않음
- 테스트 변경 내용:
  - `TextTestGoldenTest` 12케이스 신규 (ctest 자동 등록)
  - 기존 `TunerTest` 13 + `TVControllerTest` 81 유지

## 검증

- 실행한 빌드 명령:
  - `cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug`
  - `cmake --build build`
  - (golden 생성 시) `cmake -S . -B build -DUPDATE_GOLDEN_MASTER=ON` 후 `TextTestGoldenTest.exe` 실행
- 실행한 테스트 명령:
  - `ctest --test-dir build --output-on-failure`
  - `ctest --test-dir build -R TextTestFixture` (Golden만)
- 테스트 결과:
  - **93개 테스트 모두 통과** (TunerTest 13 + TVControllerTest 81 + TextTestFixture 12)
  - 실패 테스트 0개
- 커버리지 확인 결과:
  - 이번 작업에서는 gcov/lcov를 실행하지 않았다.

## GitHub 업로드

- 브랜치: `prompting`
- 커밋 메시지: `Implement TextTest golden master tests with report backup`
- 원격 저장소: `origin` (`https://github.com/jskor013/TDD_TV_13.git`)
- Push 결과: (커밋 후 갱신)
- 커밋 해시: (커밋 후 갱신)

## 주의 사항

- 남은 리스크:
  - Golden은 `std::cout` 문자열(한국어 메시지)에 의존 — 메시지 변경 시 golden 일괄 갱신 필요
  - Windows/Unix 줄바꿈은 `GoldenMaster.hpp`에서 `\r\n` → `\n` 정규화로 완화
- 후속 작업:
  - (선택) `docs/test_plan.md`에 Golden Master 섹션 반영
  - (선택) README 미커버 시나리오 golden TC 추가
  - Golden 갱신: `.\scripts\update_golden.ps1` 또는 `-DUPDATE_GOLDEN_MASTER=ON`

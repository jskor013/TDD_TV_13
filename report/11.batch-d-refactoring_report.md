# 작업 보고서: batch-d-refactoring

## 개요

- 작업 일시: 2026-05-19
- 작업 목적: `docs/code_quality_report.md` 배치 D(도메인 분리·SRP) 리팩토링을 적용하고, `Tuner.h` 미수정·테스트 Green 제약 하에 God Class를 Facade + 작은 도메인 타입으로 분리한다.
- 핵심 결과: `DigitInputBuffer`, `FavoriteStore`, `SearchSession`, `ChannelObserver` 신설, `TVController` Facade 축소, 전체 93 테스트 100% Pass.

## 변경 내용

- 수정/추가한 파일:
  - `include/DigitInputBuffer.h` — 신규 (배치 D1)
  - `include/FavoriteStore.h` — 신규 (배치 D1)
  - `include/SearchSession.h` — 신규 (배치 D2)
  - `include/ChannelObserver.h` — 신규 (배치 D3)
  - `include/TVController.h` — 배치 D1~D4 전체
  - `report/11.batch-d-refactoring_report.md` (본 보고서)
  - `prompting/11.batch-d-refactoring_prompt.md` (대화 백업)
- 주요 구현 내용:
  - **D1 (#6, #17)**: `tv::input::DigitInputBuffer` — 숫자 버퍼 FSM (`appendDigit`, `readyToAutoCommit`, `clear`). `tv::favorite::FavoriteStore` — 선호 채널 Repository (`toggle`, `nextAfter` → `ListChannelNavigator` 재사용).
  - **D2 (#6)**: `tv::search::SearchSession` — `collect(Tuner&)`, `channels()`, `empty()`. CH UP/DOWN은 `search_.empty()` 기준으로 배치 C의 `Linear` / `List` navigator 선택 유지.
  - **D3 (#9)**: `tv::observer::IChannelObserver`, `CoutChannelObserver`, `NullChannelObserver`. `commitChannel`에서 `std::cout` 제거 → `channelObserver_->onChannelCommitted()` (golden transcript 동작 유지).
  - **D4 (#6)**: `TVController`를 Facade로 축소 — 키 디스패치 + 서브시스템 조합만 담당. `#include <iostream>`, `<vector>`, `<algorithm>` 제거.
  - `Tuner.h`는 수정하지 않음.
- 테스트 변경 내용:
  - 테스트 소스·케이스 추가/수정 없음 (동작 불변 리팩토링).

## 검증

- 실행한 빌드 명령:
  - `cmake --build build`
- 실행한 테스트 명령:
  - `ctest --output-on-failure` (build 디렉터리)
- 테스트 결과:
  - 93개 테스트 모두 통과 (TunerTest 13 + TVControllerTest 74 + TextTestGoldenTest 6)
  - 실패 테스트 0개
  - 포커스 검증: `REQ_CH_*`, `REQ_FAV_*`, `REQ_SEEK_*`, `REQ_UD_*`, `TextTestFixture/*`
- 커버리지 확인 결과:
  - 이번 작업에서는 gcov/lcov 커버리지를 별도로 실행하지 않았다.

## GitHub 업로드

- 브랜치: `prompting`
- 커밋 메시지: `Refactor batch D domain separation with report backup`
- 원격 저장소: `origin` (`https://github.com/jskor013/TDD_TV_13.git`)
- Push 결과: (커밋 후 기록)
- 커밋 해시: (커밋 후 기록)

## 주의 사항

- 남은 리스크:
  - `channelObserver_`는 기본 `CoutChannelObserver` 포인터 — 외부 주입 API는 아직 없음 (배치 E에서 선택 적용 가능).
  - `SearchSession::collect`는 `Tuner&` 참조 전제 — `tuner_` null 시 UB (기존과 동일).
  - `ListChannelNavigator`가 `search_.channels()` / `FavoriteStore::channels()`에 대한 `const` 참조 보유 — variant·`visit` 즉시 사용 패턴에서 멤버 수명 내 안전.
- 후속 작업:
  - 배치 E: `.h`/`.cpp` 분리 (#10), `ChannelNumber` strong type (#8), `Tuner&` 참조화 (#12), Observer 생성자 주입 (선택)

# 작업 보고서: batch-c-refactoring

## 개요

- 작업 일시: 2026-05-19
- 작업 목적: `docs/code_quality_report.md` 배치 C(네비게이션 Strategy, OCP) 리팩토링을 적용하고, `Tuner.h` 미수정·테스트 Green 제약 하에 CH UP/DOWN 모드 분기를 `IChannelNavigator` + `std::variant`로 위임한다.
- 핵심 결과: `include/ChannelNavigator.h` 신설, `TVController.h` 네비게이션 Strategy 적용, `ListChannelNavigator`가 `cyclicNeighbor` 단일 소스 재사용, 전체 93 테스트 100% Pass.

## 변경 내용

- 수정/추가한 파일:
  - `include/ChannelNavigator.h` — 신규 (배치 C1+C2)
  - `include/TVController.h` — 배치 C1+C2 전체
  - `report/10.batch-c-refactoring_report.md` (본 보고서)
  - `prompting/10.batch-c-refactoring_prompt.md` (대화 백업)
- 주요 구현 내용:
  - **C1 (#7)**: `IChannelNavigator`, `LinearChannelNavigator`, `ListChannelNavigator` 도입. `channelNavigatorForUpDown()`이 `searchedChannels.empty()`에 따라 `std::variant` 선택. `handleChannelUp/Down`의 `if (empty)` 분기 제거 → `std::visit` 위임.
  - **C2 (#1 연계)**: `tv::navigation::cyclicNeighbor`를 `ChannelNavigator.h`로 이동(단일 소스). `ListChannelNavigator::channelUp/Down` 및 `handleFavNext`가 동일 알고리즘 재사용.
  - **부수 정리**: `tv::channel` 상수·`step()`을 `ChannelNavigator.h`로 통합(`TVController` 중복 제거). `channelUp/DownWithoutSearch`, `channelUp/DownWithSearch` 4개 private 메서드 삭제.
  - `#include <variant>` 추가, `#include <optional>`은 `ChannelNavigator.h` 경유.
  - `Tuner.h`는 수정하지 않음.
- 테스트 변경 내용:
  - 테스트 소스·케이스 추가/수정 없음 (동작 불변 리팩토링).

## 검증

- 실행한 빌드 명령:
  - `cmake --build build`
- 실행한 테스트 명령:
  - `ctest --output-on-failure` (build 디렉터리)
- 테스트 결과:
  - 93개 테스트 모두 통과 (TunerTest 13 + TVControllerTest 74 + TextTestGoldenTest 6)
  - 실패 테스트 0개
  - 포커스 검증: `REQ_UD_S*`, `ChannelWrap/*`, `REQ_FAV_N*`, `REQ_FAV_C*`, `README_ch_up_down_*`, `README_fav_next_*`
- 커버리지 확인 결과:
  - 이번 작업에서는 gcov/lcov 커버리지를 별도로 실행하지 않았다.

## GitHub 업로드

- 브랜치: `prompting`
- 커밋 메시지: `Refactor batch C navigation strategy with report backup`
- 원격 저장소: `origin` (`https://github.com/jskor013/TDD_TV_13.git`)
- Push 결과: 성공 (`1e20fe8..1ce9659` → `origin/prompting`)
- 커밋 해시: `1ce9659`

## 주의 사항

- 남은 리스크:
  - `ListChannelNavigator`는 비어 있지 않은 목록 전제로 `*cyclicNeighbor` 역참조(CH UP/DOWN은 `channelNavigatorForUpDown`에서 empty 시 Linear 선택, FAV NEXT는 empty 시 early return).
  - `ListChannelNavigator`가 `const std::vector<int>&`를 보유 — variant 생성·즉시 `visit` 패턴에서는 `searchedChannels` 멤버 수명 내에서 안전.
  - `std::cout` side effect, God Class, digit FSM 등은 배치 D~E 후속 작업 대상.
- 후속 작업:
  - 배치 D: `DigitInputBuffer`, `FavoriteStore`, `SearchSession` 분리 (SRP)
  - 배치 E: `.h`/`.cpp` 분리, `ChannelNumber` strong type, `Tuner&` 참조화
